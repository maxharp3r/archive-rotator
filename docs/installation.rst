============
Installation
============

At the command line::

    $ easy_install archive_rotator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv archive_rotator
    $ pip install archive_rotator
